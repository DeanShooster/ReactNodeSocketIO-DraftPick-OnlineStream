import { RaidLeagueLogo } from "../../../../Images";

export const VersusLogo = () => {
    return (
        <div>
           <img alt='' src={RaidLeagueLogo} draggable={false}/>
        </div>
    );
}